---
title: "Taking AI-assisted engineering to 100% of the org"
blurb: "Embedding AI tooling into daily engineering workflow across the team — and moving the delivery metrics that prove it worked."
role: "Software Technical Lead"
org: "Creoate"
period: "A 12-month programme at Creoate"
treatment: "summary"
stack:
  - "Claude"
  - "Jira"
  - "Atlassian Rovo"
  - "Devin"
  - "GitLab CI"
  - "GitHub Actions"
metrics:
  - value: "~3.5 → ~6.8"
    label: "delivery velocity"
  - value: "~60% → <15%"
    label: "change failure rate"
  - value: "~5d → ~1.5d"
    label: "median cycle time"
featured: true
order: 3
confidential: true
draft: false
---

{/* TODO(diogo): which 12 months did this run over? The CV says "over a year" but not which year — needed to state a period precisely. */}

## Context

Adopting AI tooling in an engineering team is usually reported as a licence count. That tells you nothing. The question I cared about was whether it changed what the team shipped and how often the team broke things — and whether it held up over a year rather than a fortnight.

I drove that adoption at Creoate across the engineering org, and tracked it against delivery metrics we were already measuring.

## What I built

Adoption, not tooling. The work was embedding these tools into the workflow people already had, rather than adding a step beside it: where they fit in code review, in writing tests, in working through unfamiliar parts of the codebase.

The tooling was the smaller half of the job. Alongside the hosted assistants I leaned on tools like Jira and Atlassian Rovo and coding agents like Devin, and we worked the BMAD method as the structure for how AI-assisted coding actually happens on a task. Most of the real work was organisational: defining the way we work, standardising the processes inside the team around those tools, and then defining the KPIs we'd monitor and holding the team to them.
{/* TODO(diogo): how was adoption actually driven — pairing sessions, internal guidelines, a champion per squad? "Drove adoption to 100%" needs the mechanism to be credible. */}
{/* TODO(diogo): what was the main source of resistance, and what changed it? This is the most interesting part of the story and I don't have it. */}

## Results

Over roughly a year, measured against the same metrics before and after:

- 100% team usage — every engineer in the org, not a pilot group.
- Delivery velocity roughly doubled, from ~3.5 to ~6.8.
- Change failure rate fell from ~60% to under 15%.
- Median cycle time dropped from ~5 days to ~1.5 days.

The change failure rate is the number I find most worth stating. The common objection to AI-assisted engineering is that it buys throughput by shipping more defects. Here throughput went up and failure rate went down by a factor of four over the same period, which is the opposite of that prediction.

{/* TODO(diogo): what unit is the ~3.5 → ~6.8 velocity figure in — story points per sprint, PRs merged per engineer per week? The number is less persuasive without the unit. */}
{/* TODO(diogo): were there confounders in that year — team size changes, a test-coverage push, the checkout rebuild finishing — that you'd want to acknowledge honestly? */}
