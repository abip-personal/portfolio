---
title: "Post title goes here"
description: "One or two sentences that say what the post argues. This is used on the index card and as the meta description, so write it for a stranger."
pubDate: 2026-01-15
# updatedDate: 2026-02-01   # optional — uncomment when you materially revise a published post
tags:
  - "architecture"
  - "typescript"
draft: true
---

{/*
  How to use this template.

  1. Copy it to a real slug: `cp _template.mdx my-post-slug.mdx`.
     The filename becomes the URL slug, so use lowercase words separated by hyphens.
  2. Files starting with an underscore are ignored by the content loader, so this
     template is never built or published. Do not rename it in place.
  3. Fill in the frontmatter. `title`, `description` and `pubDate` are required;
     `updatedDate`, `tags` and `draft` are optional and default to unset/[]/false.
     `pubDate` and `updatedDate` accept any date string Date can parse — YYYY-MM-DD is the house style.
  4. Leave `draft: true` while writing. Flip it to `false` to publish.
  5. Start the body at `##`. The page template renders the title as the `<h1>`,
     so a `#` heading here would give you two.
*/}

Open with the claim, not with throat-clearing. A reader should know what the post
is arguing by the end of the first paragraph.

## First section

Body copy. MDX, so standard Markdown plus JSX where it earns it.

```ts
// Code blocks are syntax highlighted.
export const example = (n: number): number => n * 2;
```

## Second section

Keep paragraphs short.

## Wrapping up

State what changed in the reader's head, and stop.
